{% if page.tags %}
    <div class="tags">
        {% for tag in page.tags %}
            <a href="{{ site.url }}/blog/tags/{{ tag|url_encode(true) }}" class="tag is-light is-borderless">
                {{- tag -}}
            </a>
        {% endfor %}
    </div>
{% endif %}
