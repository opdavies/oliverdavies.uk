{% extends "default" %}

{% block content_wrapper %}
    <div>
        <h1 class="text-xl font-bold md:text-2xl">{{ page.title }}</h1>

        <div class="mt-4">
            {{ parent() }}
        </div>
    </div>
{% endblock %}
